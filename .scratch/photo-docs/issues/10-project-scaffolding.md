# 10 — Next.js project scaffolding with ShadCN and test runner

**What to build:** Initialize the foundational Next.js fullstack application with TypeScript, Tailwind CSS, ShadCN UI components baseline, and a fast test runner (Vitest) so subsequent slices have a working app skeleton and test harness to build against.

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] Next.js app initialized with App Router and TypeScript
- [ ] Tailwind CSS and ShadCN UI base configuration initialized
- [ ] Test runner (Vitest + Testing Library) configured and executing with a passing sample test
- [ ] Clean project folder structure for components, adapters, hooks, and lib utilities
- [ ] Development server runs cleanly (`npm run dev`) and test suite runs cleanly (`npm run test`)
