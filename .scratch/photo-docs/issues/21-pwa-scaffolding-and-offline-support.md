# 21 — PWA scaffolding and service worker

**What to build:** Progressive Web App configuration including manifest, service worker caching for offline app shell and folder navigation, and background sync registration so uploads can sync smoothly across foreground/background transitions.

**Blocked by:** 16 — Background upload and connectivity detection, 17 — Error handling and retry mechanism, 19 — Nextcloud folder navigation and breadcrumbs

**Status:** ready-for-agent

- [ ] Web app manifest configured with icons, standalone display mode, and theme colors
- [ ] Service worker caches static assets and enables offline app shell loading
- [ ] Service worker background sync registered for queued upload events
- [ ] App is installable to mobile home screen (iOS & Android)
